<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.meme.mapper.MemeMapper">
    <select id="getMeme" resultType="com.meme.vo.MemeVO">
        SELECT m.*, u.username
        FROM meme m LEFT JOIN user u
        ON m.user_id = u.id
        WHERE m.id = #{id}
    </select>
    <select id="pageQuery" resultType="com.meme.vo.MemeVO">
        SELECT m.*, u.username
        FROM meme m LEFT JOIN user u
        ON m.user_id = u.id
        ORDER BY m.created_at DESC
    </select>
</mapper>